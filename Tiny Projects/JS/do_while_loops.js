/* Change the while loop in the code to a do...
while loop so the loop will push only the number 10 to myArray, 
and i will be equal to 11 when your code has finished running. */

const myArray = [];
let i = 10;

do {
  myArray.push(i); //I.e.: do this action while the condition i < 5. This will only run until i is no longer less than 5.
  i++;
  }
  while (i < 5);
