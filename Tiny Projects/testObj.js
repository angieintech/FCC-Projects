const testObj = {
  "an entree": "hamburger",
  "my side": "veggies",
  "the drink": "water"
};

const entreeValue = testObj["an entree"];   // access the object using bracket notation.
const drinkValue = testObj["the drink"];    
